#pragma once
#include <iostream>
#include <array>

using namespace std;

template <class Stack>
class Stack
{
	int size
	int *top;
	auto [] s;
public:
	//construtor
	Stack(int x): size(x){
		if (x <= 0)
			throw "Tamanho negativo!";
		else
			s
			s = new auto[x];
	}

	//destrutor
	~Stack() {
		for (int i = 0; i < s ; i++)
		{
			
		}
	}


};

